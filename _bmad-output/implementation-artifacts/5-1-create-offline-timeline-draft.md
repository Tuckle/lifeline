# Story 5.1: Create Offline Timeline Draft

Status: done

## Story

As an authenticated Lifeline user,  
I want to draft a memory while temporarily offline,  
so that I can capture important life material without waiting for connectivity.

## Acceptance Criteria

1. Given I am authenticated and temporarily offline, when I choose Add memory, then I can create a local draft with the mandatory event fields supported by MVP, and the app clearly labels the draft as saved on this device or local only.
2. Given I create an offline draft, when the draft is stored locally, then it preserves title, date/approximate date, and any other mandatory event fields supported for offline editing, and it does not require imports, media upload, or full reflection sync while offline.
3. Given I return to the timeline while offline, when local drafts are visible, then the draft appears in the timeline or draft area with an explicit unsynced/local state, and the state is not communicated by color alone.
4. Given local draft storage fails, when I attempt to save, then I see a clear recoverable error, and the app does not falsely claim the draft was saved.
5. Given I refresh the app while still offline, when I return to Lifeline, then locally stored drafts remain available where practical, and the app distinguishes local drafts from synced timeline events.

## Tasks / Subtasks

- [x] Add local draft storage. (AC: 1, 2, 4, 5)
  - [x] Store mandatory draft fields in `localStorage`.
  - [x] Preserve title and date/approximate date fields.
  - [x] Show recoverable storage errors.

- [x] Add offline draft UI. (AC: 1, 2, 3, 5)
  - [x] Add local-only draft form to Add memory.
  - [x] Show drafts in a local draft area with text status labels.
  - [x] Keep imports, media, and reflection sync out of this story.

- [x] Extend validation and verify. (AC: 1-5)
  - [x] Update `scripts/validate-foundation.mjs` for local draft storage, status labels, and no server sync.
  - [x] Run `npm run typecheck`, `npm run lint`, `npm test`, `npm audit --omit=dev`, `npm run build`, and route smoke checks.

## Dev Notes

- Story 4.7 is done and pushed to `origin/main` at commit `0bed57c`.
- This story starts Epic 5 and should mark Epic 5 in-progress.
- Keep the first slice local-only. Server sync and conflict recovery begin in later stories.
- Do not add import/media/reflection offline support.

## Dev Agent Record

### Agent Model Used

GPT-5

### Debug Log References

- `npm test`
- `npm run typecheck`
- `npm run lint`
- `npm audit --omit=dev`
- `npm run build`
- `curl` route smoke checks for `/add`, `/timeline`, and `/auth/login?next=%2Fadd`

### Completion Notes List

- Story context generated by BMad Create Story workflow on 2026-05-06.
- Added local-only offline memory draft panel on the Add page.
- Stored mandatory draft fields in `localStorage`: title, date precision, exact date, and approximate period.
- Added visible text labels for `Local only`, `Unsynced`, and saved-on-this-device state.
- Added recoverable local storage and validation errors without falsely claiming drafts were saved.
- Added validation guardrails to keep Story 5.1 local-only with no import/media/reflection/server sync.

### File List

- `/Users/Tuckle/Projects/lifeline/_bmad-output/implementation-artifacts/5-1-create-offline-timeline-draft.md`
- `/Users/Tuckle/Projects/lifeline/_bmad-output/implementation-artifacts/sprint-status.yaml`
- `/Users/Tuckle/Projects/lifeline/scripts/validate-foundation.mjs`
- `/Users/Tuckle/Projects/lifeline/src/app/(workspace)/add/page.tsx`
- `/Users/Tuckle/Projects/lifeline/src/features/offline/components/offline-memory-draft-panel.tsx`

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-05-06 | 0.1 | Created implementation story context for offline memory drafts. | Amelia (Dev Agent) |
| 2026-05-06 | 1.0 | Implemented and verified local-only offline memory draft creation. | Amelia (Dev Agent) |
